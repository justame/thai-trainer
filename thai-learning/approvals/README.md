# Approval receipts

This directory stays empty until the user approves an exact lesson revision. Approval
receipts are immutable evidence; editing a lesson creates a new revision and a new
receipt rather than changing an existing receipt.

An approval receipt never authorizes a paid request. Paid TTS authorization is stored
separately under `authorizations/`.
