# Paid TTS authorization receipts

This directory stays empty until the user separately authorizes a paid TTS request for
an already-approved lesson revision and content hash.

Never infer this authorization from sentence approval. Never reuse it after any text,
order, voice, provider, or audio-program change.
